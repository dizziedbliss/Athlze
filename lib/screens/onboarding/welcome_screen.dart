import 'package:flutter/material.dart';

class OnboardingWelcomeCards extends StatelessWidget {
  const OnboardingWelcomeCards({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
      ],
    );
  }
}
